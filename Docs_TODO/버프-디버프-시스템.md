# 버프/디버프 시스템 — 미구현 항목

기본 버프/디버프/면역/반사반격/민첩 시스템은 구현 완료.
범위 공격 다중 타겟 순차 처리도 구현 완료.
아래는 아직 구현되지 않은 항목들.

---

## 1. 디버프 반사 실제 구현

현재 `reflect_debuff`는 로그만 출력하고 실제 디버프 되돌리기는 미구현.

### 구현 포인트

- 디버프 적용 시 대상에 `reflect_debuff` 상태가 있으면 공격자에게 같은 디버프를 적용
- 반사 유닛끼리 만났을 때 무한 반사 방지 로직

---

## 2. 미결정 사항

- [ ] UI에서 상태 효과 아이콘 표시 (현재 텍스트로 표시 중 — CharacterInfo.tsx)

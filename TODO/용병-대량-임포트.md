# 용병 대량 임포트 (Phase 4-3)

> RE 프로젝트에서 생성된 데이터로 용병 325개를 웹앱에 등록하는 작업.
> 관련 문서: `Docs/전투구현/용병-추가-가이드.md`

---

## 소스 데이터

| 파일 | 위치 | 내용 |
|------|------|------|
| `mercenaries.json` | `app-brown-reverse-engineering/analysis/game-data/webapp-export/` | 325개 (3★=54, 4★=118, 5★=153) |

---

## 즉시 가능한 것

- [ ] `create-mercenary` 스크립트로 325개 `.ts` 스캐폴딩 일괄 생성 (skills 빈 배열)
  ```bash
  cd app-brown-dust
  npm run create-mercenary -- --config ../config.json
  ```
- [ ] 기존 10개 용병 스탯을 RE 원본과 대조·수정
  - **아리네스**: RE에서 `attacker`(typeRaw=1) → 웹앱은 `support` — 확인 필요
  - **리즈넷**: RE에서 `Rigenette` — 이름 불일치, 수동 확인 필요
- [ ] 포함 가능한 스탯: `maxHp`, `atk`, `def`, `critRate`, `critDamage`, `agility`, `supportPower`(58개)

## 불가능한 것 / 수동 작업 필요

| 항목 | 이유 |
|------|------|
| **skills 배열** | skillCode만 있고 효과 매핑 별도 작업 필요 → `스킬-자동-연결.md` |
| **attackTarget / attackRange / rangeSize** | 데이터에 없음, 용병별 수동 지정 |
| **standalone 6★** 318개 | 태생 성급 불명으로 제외 (`백로그/standalone-6star.md`) |
| **이미지 파일** | `portraits/`, `thumbnails/` 없는 용병은 `raw/assets/`에서 별도 복사 |
| **piercing, patience** | 전부 0 — 현재 스탯 체계에 없음 |

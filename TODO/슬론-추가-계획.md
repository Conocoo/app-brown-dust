# 슬론 (Sloan) — 3성 방어형 용병 추가 계획

## 용병 기본 정보

| 항목 | 값 |
|------|-----|
| ID | `sloan` |
| 이름 | 슬론 |
| 타입 | defender |
| 성급 | 3 |
| maxHp | 5574 |
| atk | 896 |
| def | 10 |
| critRate | 5 |
| critDamage | 50 |
| agility | 0 |
| emoji | 🛡️ |
| imageId | (미정) |

## 스킬 목록

### 1. 상급 중독 반격 (`advanced_poison_counter`)

| 항목 | 값 |
|------|-----|
| timing | `passive` (전투 시작 전) |
| target | `self` |
| 설명 | 적에게 중독 피해를 반격. 적의 일반 공격에 회복 효과 획득 |
| 예외 | 복제 가능 |

**effects:**
- `{ type: 'poison_counter', value: 0, duration: 999, buffType: 'special' }` — 중독 반격 (피격 시 중독 부여)
- `{ type: 'on_hit_recovery', value: 0, buffType: 'special' }` — 피격 시 회복 효과

> ⚠️ 신규 이펙트 타입 필요: `poison_counter`, `on_hit_recovery`
> 전투 로직(turn.ts / damage.ts) 수정 필요

### 2. 중독 (`poison`)

| 항목 | 값 |
|------|-----|
| timing | `before_attack` (일반 공격 받기 전) |
| target | `enemy_front` |
| duration | 6턴 |
| 설명 | 매 턴 적 공격력 × 12% 지속피해. 공격형 대상 3배 |
| 예외 | 중첩 적용 가능 |

**effects:**
- `{ type: 'poison', value: 12, atkScaling: true, duration: 6, debuffClass: 'dot' }`

> ⚠️ 기존 `burn`과 유사하나 별도 타입. 공격형 3배 증가 로직 필요
> 중첩 가능 처리 필요

### 4. 상급 보호막 (`advanced_shield`)

| 항목 | 값 |
|------|-----|
| timing | `after_attack` (일반 공격 후) |
| target | `self` |
| duration | 12턴 |
| 설명 | 받는 피해량 -35% |

**effects:**
- `{ type: 'shield', value: 35, duration: 12, buffType: 'shield' }`
- 하위 스킬: `triggerSkill: 'purify_dot'` — 고통 정화 발동

> ✅ 기존 `shield` 스킬과 유사 — 오버라이드 또는 별도 스킬로 처리
> 고통 정화를 triggerSkill로 연결

### 5. 고통 정화 (`purify_dot`)

| 항목 | 값 |
|------|-----|
| timing | `after_attack` (일반 공격 후) |
| target | `self` |
| duration | 즉시 |
| 설명 | 적용되어 있는 지속 피해 스킬 타입을 제거 |

**effects:**
- `{ type: 'purify_dot', value: 0 }` — DoT 디버프 전체 제거

> ⚠️ 신규 이펙트 타입 필요: `purify_dot`
> 전투 로직에서 debuffClass: 'dot'인 버프를 전부 제거하는 처리 필요

### 6. 부패 (`decay`)

| 항목 | 값 |
|------|-----|
| timing | `after_attack` (일반 공격 후) |
| target | `enemy_front` |
| duration | 10턴 |
| 설명 | 매 턴 적 최대생명력 × 2.5% 지속피해 |
| 예외 | 중첩 적용 가능 |

**effects:**
- `{ type: 'decay', value: 2.5, duration: 10, debuffClass: 'dot' }`

> ⚠️ 신규 이펙트 타입 필요: `decay`
> `burn`/`poison`과 유사하나 최대HP 기반 스케일링. 중첩 가능

### 7. 회복 (`recovery`)

| 항목 | 값 |
|------|-----|
| timing | `before_attack` (일반 공격 받기 전) |
| target | `self` |
| duration | 즉시 |
| 설명 | 최대생명력 × 10% 즉시회복 |

**effects:**
- `{ type: 'recovery', value: 10, duration: 0 }`

> ✅ 기존 `recovery` 스킬 이미 존재 — 오버라이드로 재사용 가능한지 확인 필요

## 구현 작업 목록

- [ ] 스킬 파일 생성 (신규 스킬만)
  - [ ] `advanced-poison-counter.ts` — 신규
  - [ ] `poison.ts` — 신규
  - [ ] `advanced-shield.ts` — 신규 (또는 기존 shield 오버라이드 검토)
  - [ ] `purify-dot.ts` — 신규 (고통 정화, 상급 보호막의 하위 스킬)
  - [ ] `decay.ts` — 신규 (부패)
  - [ ] `recovery` — 기존 스킬 재사용 검토
- [ ] 스킬 레지스트리 등록 (`src/data/skills/index.ts`)
- [ ] 전투 로직 수정 (신규 이펙트 처리)
  - [ ] `poison` DoT 처리 (burn 유사, 공격형 3배)
  - [ ] `poison_counter` 반격 로직
  - [ ] `on_hit_recovery` 피격 시 회복
  - [ ] `purify_dot` DoT 디버프 일괄 제거
  - [ ] `decay` DoT 처리 (최대HP 기반, 중첩 가능)
- [ ] 용병 파일 생성 (`src/data/mercenaries/3star/sloan.ts`)
- [ ] 용병 레지스트리 등록 (`src/data/mercenaries/index.ts`)
- [ ] 문서 업데이트
  - [ ] `Docs/스킬-목록.md`
  - [ ] `Docs/폴더-구조.md`
- [ ] `npm run validate` 타입 체크 통과

## 비고

- 스킬을 추가로 더 가져온 뒤 한번에 구현 예정
- `recovery`는 기존 스킬과 ID 충돌 가능 — 슬론 전용이면 `sloan_recovery` 등으로 분리 필요
- 상급 중독 반격은 완전히 새로운 메커니즘이므로 전투 로직 변경 범위가 큼

# 줄리 (Julie) — 3성 지원형 용병 추가 계획

## 용병 기본 정보

| 항목 | 값 |
|------|-----|
| ID | `julie` |
| 이름 | 줄리 |
| 타입 | support |
| 성급 | 3 |
| maxHp | 2360 |
| atk | 0 |
| supportPower | 185.48 |
| def | 0 |
| critRate | 0 |
| critDamage | 0 |
| agility | 25 |
| emoji | 💚 |
| imageId | (미정) |
| attackRange | `area_n` |
| rangeSize | 3 |

> 지원 범위: 메인 타겟 기준 3×3

## 스킬 목록

### 1. 상급 구속 정화 (`advanced_cc_purify`)

| 항목 | 값 |
|------|-----|
| timing | `after_attack` (아군 지원 시) |
| target | `next_ally` |
| duration | 즉시 |
| 설명 | 아군에게 적용되어 있는 공격 방해 스킬 타입(CC)을 제거 |

**effects:**
- `{ type: 'purify_cc', value: 0 }` — CC 디버프 전체 제거

> ⚠️ 신규 이펙트 타입 필요: `purify_cc`
> debuffClass: 'cc'인 디버프를 전부 제거하는 처리 필요

### 2. 상급 재생 부여 (`advanced_regeneration_grant`)

| 항목 | 값 |
|------|-----|
| timing | `after_attack` (아군 지원 시) |
| target | `next_ally` |
| duration | 14턴 |
| 설명 | 매 턴 아군 최대생명력 × 10% 회복 + 즉시 최대생명력 × 5% 회복 |

**effects:**
- `{ type: 'regeneration', value: 10, spScaling: true, duration: 14, buffType: 'stat_enhance' }` — 매 턴 회복
- `{ type: 'recovery', value: 5, duration: 0 }` — 즉시 회복

> 제이든의 `regeneration`과 동일 타입 재사용 가능
> spScaling 여부 확인 필요 (지원력 비례인지 고정값인지)

### 3. 반사 신경 부여 (`reflex_grant`)

| 항목 | 값 |
|------|-----|
| timing | `after_attack` (아군 지원 시) |
| target | `next_ally` |
| duration | 12턴 |
| 설명 | 아군 민첩성 증가 (지원력 × 50%). 적의 일반 공격에 방어력 증가 효과 획득 |
| 예외 | 약탈 및 복사에 영향을 받지 않음 |
| 하위 스킬 | 방어력 증가 (`reflex_def_up`) — 피격 시 발동 |

**effects:**
- `{ type: 'agility_up', value: 50, spScaling: true, duration: 12, buffType: 'stat_enhance', ignoreImmunity: true }` — 민첩성 증가
- `triggerSkill: 'reflex_def_up'` — 피격 시 방어력 증가

> ⚠️ 신규 이펙트 타입 필요: `agility_up`
> 약탈/복사 면역 처리 필요 (신규 속성 또는 기존 메커니즘 확인)

### 4. 방어력 증가 (`reflex_def_up`)

| 항목 | 값 |
|------|-----|
| timing | `after_attack` (일반 공격 받은 후) |
| target | `next_ally` |
| duration | 1턴 |
| 조건 | 반사 신경 부여 보유 시 피격당하면 발동 |
| 설명 | 방어력 +100% |

**effects:**
- `{ type: 'def_up', value: 100, duration: 1, buffType: 'stat_enhance' }`

> ⚠️ 피격 트리거 메커니즘 필요 (on_hit)

## 구현 작업 목록

- [x] 스킬 파일 생성
  - [x] `advanced-cc-purify.ts` — 신규
  - [x] `advanced-regeneration-grant.ts` — 신규
  - [x] `reflex-grant.ts` — 신규
  - [x] `reflex-def-up.ts` — 신규 (반사 신경 하위)
- [x] 스킬 레지스트리 등록 (`src/data/skills/index.ts`)
- [x] 전투 로직 수정
  - [x] `purify_cc` CC 디버프 일괄 제거
  - [x] `agility_up` 민첩성 증가 처리
  - [x] 피격 시 triggerSkill 발동 (on_hit)
  - [x] 약탈/복사 면역 속성
- [x] 용병 파일 생성 (`src/data/mercenaries/3star/julie.ts`)
- [x] 용병 레지스트리 등록 (`src/data/mercenaries/index.ts`)
- [x] 문서 업데이트

## 비고

- `regeneration` 타입은 제이든과 공유 가능
- 반사 신경의 "약탈/복사 면역"은 새로운 메커니즘 — 구현 범위 확인 필요
- 지원력(supportPower) 185.48%는 소수점 포함 — 기존 용병(아리네스 203) 참고

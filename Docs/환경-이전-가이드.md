# 환경 이전 가이드

## 현재 상태 (2026-03-15)

### Git 상태
- **현재 브랜치**: `main`
- **리모트**: `https://github.com/Conocoo/app-brown-dust.git`
- **최근 작업**: 용병/스킬 도감 UI 리디자인, 위글 이미지 수정
- **상태**: clean (미커밋 변경 없음)
- **push 완료**: origin에 동기화됨

### 브랜치 현황
| 브랜치 | 상태 |
|---|---|
| `main` | 안정 브랜치 (현재 작업 브랜치) |
| `feature/skill-updates` | PR #1로 main에 merge 완료 |
| `feature/wiggle-mercenary` | PR #2로 main에 merge 완료 |

### 이번 세션에서 한 작업
1. `feature/wiggle-mercenary` 브랜치 — PR #2로 main에 merge 완료
2. 용병/스킬 도감 UI 리디자인
3. 위글 이미지 수정
4. 승패 판정 수정 (서포터 제외)
5. 신규 용병 3종 추가 계획서 작성 (슬론/제이든/줄리)

---

## 새 환경 셋업 절차

```bash
# 1. 클론
git clone https://github.com/Conocoo/app-brown-dust.git
cd app-brown-dust

# 2. 의존성 설치
npm install

# 3. 개발 서버 실행
npm run dev

# 4. 타입 체크 확인
npm run validate
```

## 프로젝트 주요 커맨드
| 커맨드 | 용도 |
|---|---|
| `npm run dev` | 개발 서버 |
| `npm run build` | 타입 체크 + 빌드 |
| `npm run validate` | 타입 체크만 (빠른 검증) |
| `npm run create-skill -- --config <file>` | 스킬 자동 생성 |
| `npm run create-mercenary -- --config <file>` | 용병 자동 생성 |

## 핵심 참고 문서
- `CLAUDE.md` — 프로젝트 전체 규칙 및 아키텍처 (Claude Code 필독)
- `Docs/스킬-목록.md` — 스킬 목록
- `Docs/폴더-구조.md` — 프로젝트 구조
- `Docs/전투-시스템-가이드.md` — 전투 로직 상세
- `TODO/` — 작업 계획서

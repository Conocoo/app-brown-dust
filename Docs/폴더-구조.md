# 폴더 구조

```
app-brown-dust/
├── Docs/                          # 프로젝트 설명서 (지금 보고 있는 폴더, 배포 가이드 포함)
├── TODO/                          # 설계 문서 (완료된 항목은 Docs/로 통합 후 삭제됨)
├── node_modules/                  # npm이 설치한 라이브러리들 (직접 수정 X)
├── public/                        # 정적 파일 (빌드 시 그대로 복사됨)
│   └── images/                    # 이미지 리소스 (용병 일러스트 등)
├── dist/                          # 빌드 결과물 (npm run build 후 생성)
├── src/                           # 소스 코드 (실제 개발하는 곳)
│   ├── types/
│   │   ├── game.ts                # 게임 타입 (BattleCharacter, 전투 로그, 게임 상태)
│   │   ├── skill.ts               # 스킬 타입 (Skill, SkillEffect, SkillTiming)
│   │   └── mercenary.ts           # 용병 타입 (MercenaryTemplate, StarRating)
│   ├── data/
│   │   ├── skills/                # 스킬 데이터
│   │   │   ├── index.ts           # 스킬 레지스트리 (register, getSkillById, getAllSkills)
│   │   │   ├── advanced-burn.ts   # 상급 화상 (ATK×25% DoT + 받피증 +20%, 5턴)
│   │   │   ├── advanced-fatal-strike.ts  # 상급 극강의 일격 (ATK×50% 추가피해)
│   │   │   ├── fatal-strike-recovery.ts # 회복 — 극강의 일격 (킬 시 HP×50% 회복)
│   │   │   ├── advanced-taunt.ts # 상급 도발 (도발 12턴 + 받피량 -35%)
│   │   │   ├── advanced-armor-strike.ts # 상급 철갑의 필격 (DEF×ATK×275% 추가피해)
│   │   │   ├── recovery.ts       # 회복 (킬 시 HP×100% 회복)
│   │   │   ├── advanced-dot-immune.ts # 상급 지속 피해 면역 (DoT 면역 + 받피량 -50%)
│   │   │   ├── shield.ts         # 보호막 (자신, 받피량 -25%, 12턴)
│   │   │   ├── advanced-focus-fire.ts  # 상급 일점사 (일점사 + 전스탯 -30%, 6턴)
│   │   │   ├── taunt-immune.ts   # 도발 무시 (패시브, 영구)
│   │   │   ├── advanced-dispel.ts  # 상급 무효화 (버프 제거 + buff_block 연쇄)
│   │   │   ├── buff-block.ts     # 이로운 효과 금지 (새 버프 차단, 6턴)
│   │   │   ├── atk-weaken.ts     # 공격력 감소 (ATK -50%, 24턴)
│   │   │   ├── fair-and-square.ts # 정정당당 (임시생명력 + 공격력 증가, 연동, 16턴)
│   │   │   ├── advanced-atk-up.ts # 상급 공격력 증가 부여 (ATK + 치명확률, 12턴)
│   │   │   ├── cc-immune.ts      # 공격 방해 면역 (CC 면역, 8턴)
│   │   │   ├── stat-debuff-immune-grant.ts # 능력치 약화 면역 부여 (8턴)
│   │   │   ├── advanced-stun.ts  # 상급 기절 (기절 + 받피증 +65%, 12턴)
│   │   │   ├── advanced-precision-strike.ts # 상급 적중의 강타 (치명확률×ATK×150%)
│   │   │   ├── crit-damage-sustained-increase.ts # 치명피해 지속 증가 (매 턴 +15%)
│   │   │   ├── dispel.ts         # 무효화 (버프 제거)
│   │   │   ├── supreme-fatal-strike.ts # 최상급 극강의 일격 (ATK×200%)
│   │   │   ├── on-kill-atk-up.ts # 공격력 증가 — 적 사망 시 (ATK +100%)
│   │   │   ├── curse.ts          # 저주 (전스탯 -65%, 18턴)
│   │   │   ├── burn.ts           # 화상 (ATK×4% DoT, 5턴, 중첩 가능)
│   │   │   ├── harmful-effect-immunity.ts # 해로운 효과 면역 (모든 디버프 면역, 영구)
│   │   │   ├── advanced-selfdestruct-stun.ts # 상급 자폭 기절 (기절 + 받피증 +50%, 18턴)
│   │   │   ├── poison.ts         # 중독 (ATK×12% DoT, 6턴, 공격형 3배, 중첩 가능)
│   │   │   ├── decay.ts          # 부패 (최대HP×2.5% DoT, 10턴, 중첩 가능)
│   │   │   ├── advanced-poison-counter.ts # 상급 중독 반격 (피격 시 중독 + 회복, 패시브)
│   │   │   ├── advanced-shield.ts # 상급 보호막 (받피량 -35%, 12턴, 고통 정화 트리거)
│   │   │   ├── purify-dot.ts     # 고통 정화 (DoT 디버프 전체 제거)
│   │   │   ├── sloan-recovery.ts # 회복 — 슬론 (최대HP×10% 즉시회복)
│   │   │   ├── advanced-giant-strike.ts # 상급 거인의 일격 (최대HP×40% 추가피해, 킬 시 회복 트리거)
│   │   │   ├── giant-strike-recovery.ts # 회복 — 거인의 일격 (킬 시 HP×100% 회복)
│   │   │   ├── crit-rate-sustained-increase.ts # 치명확률 지속 증가 (매 턴 +4%)
│   │   │   ├── regeneration.ts  # 재생 (매 턴 최대HP×14% 회복, 15턴)
│   │   │   ├── advanced-cc-purify.ts # 상급 구속 정화 (CC 디버프 전체 제거)
│   │   │   ├── advanced-regeneration-grant.ts # 상급 재생 부여 (매 턴 최대HP×10% + 즉시 5%)
│   │   │   ├── reflex-grant.ts # 반사 신경 부여 (민첩 +50%sp, 피격 시 방어력 증가)
│   │   │   └── reflex-def-up.ts # 방어력 증가 (반사 신경 피격 시, +100%, 1턴)
│   │   └── mercenaries/           # 용병 데이터 (성급별 폴더)
│   │       ├── index.ts           # 용병 레지스트리 (getMercenaryById, getAllMercenaries)
│   │       ├── 3star/             # 3성 용병
│   │       │   ├── arines.ts      # 아리네스 (지원형)
│   │       │   ├── carlson.ts     # 칼슨 (방어형)
│   │       │   ├── cordelia.ts    # 코델리아 (공격형)
│   │       │   ├── liznet.ts      # 리즈넷 (공격형)
│   │       │   ├── maria.ts       # 마리아 (마법형)
│   │       │   ├── maya.ts        # 마야 (공격형)
│   │       │   ├── wiggle.ts     # 위글 (공격형, 자폭)
│   │       │   ├── sloan.ts      # 슬론 (방어형)
│   │       │   ├── jayden.ts     # 제이든 (마법형)
│   │       │   └── julie.ts     # 줄리 (지원형)
│   │   └── runeValues.ts          # 룬 수치 상수 (메인/부가 옵션 기준값)
│   ├── logic/
│   │   ├── battle.ts              # 전투 루프 (simulateBattle)
│   │   ├── targeting.ts           # 타겟 선택 (resolveEnemyTarget, getTargetsInRange, findNextAlly)
│   │   ├── damage.ts              # 데미지 계산 (버프/디버프/보호막 반영)
│   │   ├── turn.ts                # 턴 실행 (CC/면역/반사반격/버프/디버프 처리)
│   │   └── rune.ts                # 룬 스탯 적용 (applyRunes)
│   ├── components/
│   │   ├── Board.tsx              # 3x12 격자 보드 (양 팀 사이 간격 포함)
│   │   ├── Cell.tsx               # 보드의 한 칸 (HP 바, 쓰러짐 X, 우클릭/드래그 지원)
│   │   ├── CharacterList.tsx      # 용병 선택 목록 (카드 형태, 드래그 배치)
│   │   ├── CharacterInfo.tsx       # 캐릭터 정보 패널 (3단 레이아웃: 초상화, 스탯/룬, 스킬 슬롯)
│   │   ├── OrderSetter.tsx        # 공격 순서 지정 UI (좌클릭 부여, 우클릭 해제)
│   │   ├── BattleLog.tsx          # 전투 로그 (최신 상단, 팀 구분, 속도 제어)
│   │   ├── MercenaryDex.tsx       # 용병 도감 (카드 그리드, 타입/성급 필터, 모달 디테일)
│   │   └── SkillDex.tsx           # 스킬 도감 (카테고리 사이드바, 스킬 카드 리스트, 사용용병 표시)
│   ├── App.tsx                    # 메인 게임 화면 (상태 관리, 게임 흐름)
│   ├── App.css                    # 게임 화면 스타일
│   ├── index.css                  # 기본 스타일 (배경색, 폰트)
│   ├── main.tsx                   # 앱 시작점 (React를 HTML에 연결)
│   └── vite-env.d.ts              # Vite 타입 선언 (자동 생성, 수정 X)
├── scripts/                       # 자동화 스크립트
│   ├── create-skill.mjs           # 스킬 자동 생성 (JSON 설정 기반)
│   └── create-mercenary.mjs       # 용병 자동 생성 (JSON 설정 기반)
├── index.html                     # HTML 페이지 (React 앱이 여기에 렌더링됨)
├── package.json                   # 프로젝트 설정 (이름, 라이브러리 목록, 실행 명령어)
├── tsconfig.json                  # TypeScript 설정
├── vite.config.ts                 # Vite 설정
├── README.md                      # 프로젝트 소개 (GitHub 메인 페이지)
└── CLAUDE.md                      # AI 어시스턴트용 지시 파일 (게임과 무관)
```

## 핵심 파일 요약

| 파일 | 한 줄 설명 |
|------|-----------|
| `src/types/game.ts` | BattleCharacter, StatusEffect, AttackTargetType, AttackRange, BuffType, DebuffClass, 전투 로그, 게임 상태 |
| `src/types/skill.ts` | 스킬 정의 (Skill, SkillEffect + duration/buffType/debuffClass/ignoreImmunity/triggerSkill) |
| `src/types/mercenary.ts` | 용병 템플릿 (MercenaryTemplate + agility, attackTarget, attackRange, rangeSize) |
| `src/data/skills/index.ts` | 스킬 레지스트리 (Map 기반 ID 조회) |
| `src/data/mercenaries/index.ts` | 용병 레지스트리 (getMercenaryById, getAllMercenaries, getMercenariesByStar) |
| `src/data/runeValues.ts` | 룬 수치 상수 (메인/부가 옵션 기준값, 이 파일만 수정하면 룬 밸런스 조정 가능) |
| `src/logic/battle.ts` | 전투 루프 (라운드 진행, passive 스킬 발동, statusEffects 초기화) |
| `src/logic/targeting.ts` | 타겟 선택 (행 기반 순환 탐색, 범위 대상 목록 계산) |
| `src/logic/damage.ts` | 데미지 계산 (버프/디버프 반영 스탯, 보호막 곱연산, 받는 피해량 증가) |
| `src/logic/turn.ts` | 턴 실행 (CC/면역 판정, 상태 효과, 무효화/정화, 반사반격, 이로운→해로운 순서) |
| `src/logic/rune.ts` | 룬 스탯 적용 (BattleCharacter 생성 시 룬 옵션을 기본 스탯에 반영) |
| `src/components/Board.tsx` | 3x12 격자 보드 (플레이어 6칸 + 간격 + 적 6칸) |
| `src/components/Cell.tsx` | 격자 한 칸 (이모지, HP 바, 캐스팅/쓰러짐 표시) |
| `src/components/CharacterInfo.tsx` | 캐릭터 정보 패널 (3단 레이아웃: 초상화, 스탯/룬, 스킬 슬롯 + 툴팁) |
| `src/components/CharacterList.tsx` | 배치 단계에서 용병 선택 카드 목록 |
| `src/components/OrderSetter.tsx` | 공격 순서 지정 UI |
| `src/components/BattleLog.tsx` | 전투 로그 (최신 상단, 팀 구분, 라운드 구분선) |
| `src/components/MercenaryDex.tsx` | 용병 도감 (카드 그리드, 타입/성급 필터, 모달 디테일) |
| `src/components/SkillDex.tsx` | 스킬 도감 (카테고리 사이드바, 스킬 카드 리스트, 사용용병 표시) |
| `src/App.tsx` | 메인 파일 (게임 흐름, 우클릭/드래그, 전투 속도 제어) |
